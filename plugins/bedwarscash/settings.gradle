plugins {
    // Auto-download the JDK 25 toolchain for Paper 26.1 (Gradle itself runs on JDK 23 in Docker).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'bedwarscash'
